opencv-python>=4.8.0
mediapipe==0.10.14
numpy>=1.24.0,<2.0
scipy>=1.10.0
tqdm>=4.65.0

# Optional only if you later use --detector yolo or --detector hybrid with a trained hardware_model.pt
# ultralytics>=8.0.0